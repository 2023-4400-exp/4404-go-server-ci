apiVersion: v1
kind: Service
metadata:
  labels:
    app: go-serverser-ci
  name: go-serverser-ci
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: go-serverser-ci
  type: NodePort
